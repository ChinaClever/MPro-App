

Redis是一个日志型Key-Value数据库，支持网络、可基于内存亦可持久化。启用远程字典服务，需要在主机上设置好唯一的键(key)，并且数据存储采用哈希表的方式。Redis hash 是一个 string 类型的 field（字段）和value（值）的映射表，其中字段0~9 0表示主机，1表示副机1，同时支持设置数据过期额时间，默认为60秒。此外，还支持订阅频道进行设备的设置功能。



